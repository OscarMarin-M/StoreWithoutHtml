export interface ProductInterface {
  id: string;
  name: string;
  description: string;
  stock: number;
  price: number;
  active: boolean;
  categoryId: string;
}
export interface ProductInterface {
}
